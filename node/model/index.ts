export type FilmModel = {
  title: string;
  length: number;
  description: string;
  film_id: number;
};

export type ActorModel = {
  actor_id: string;
  first_name: string;
  last_name: string;
};
